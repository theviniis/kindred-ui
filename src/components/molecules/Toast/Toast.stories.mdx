import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { Toast } from './';
import * as templates from './Toast.stories.tsx';

<Meta
  title="Components/Molecules/Toast"
  component={Toast}
  argTypes={
  {
    status: {
      name: 'Toast',
      description: 'Toast are used to display an visual feedback to user',
    }
  }
}
/>

# Toast

<ArgsTable of={Toast} />

# <a id="examples"></a>Examples

### Default

<Canvas>
  <Story story={templates.Default} />
</Canvas>
